import 'package:flutter/material.dart';

class AllParticipentsScreen extends StatelessWidget {
  const AllParticipentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}